import 'package:flutter/material.dart';

class SliderFooterComponent extends StatelessWidget {
  const SliderFooterComponent({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Footer"),
    );
  }
}
